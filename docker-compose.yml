version: '3.8'

services:
  localstack:
    image: localstack/localstack:latest #requer login, senha, token
    container_name: localstack
    ports:
      - "4566:4566"
    environment:
      - SERVICES=s3,sqs
      - DEBUG=1
      - AWS_DEFAULT_REGION=us-east-1
      - LOCALSTACK_AUTH_TOKEN=ls-KixujILO-zaCU-hiki-CihE-509712881000
    volumes:
      - "/var/run/docker.sock:/var/run/docker.sock"